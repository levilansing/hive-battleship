# Task: Implement game logic and win conditions

## Description

Build turn-based game flow: player attacks → AI attacks → check win. Detect hits, track ship damage, identify sunk ships. Handle game over state when all ships destroyed. Add ship status display.

## Acceptance Criteria

Turn-based gameplay works correctly. Game detects when ships are sunk and announces it. Game ends when all player or AI ships are destroyed. Winner is displayed.

## Files to Modify

src/App.tsx, src/components/ShipStatus.tsx (new), src/utils/gameRules.ts (new), src/hooks/useGameBoard.ts, src/types/game.ts

## Instructions

1. Implement the task as described above.
2. Follow the project conventions.
3. Commit your work with a descriptive message.
4. Exit when done.
